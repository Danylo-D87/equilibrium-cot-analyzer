/**
 * @deprecated — Use `useCotStore` from `@/apps/cot/store/useCotStore` directly.
 * This re-export exists for backward compatibility only.
 */
export { useCotStore as useAppStore } from '../apps/cot/store/useCotStore';
