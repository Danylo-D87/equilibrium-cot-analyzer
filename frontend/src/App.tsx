/**
 * Legacy App.jsx — no longer the entry point.
 *
 * The application now uses React Router (see src/router.jsx).
 * Entry is via src/main.jsx → RouterProvider.
 *
 * This file is kept as a convenience re-export.
 * It can be safely deleted.
 */
export { router } from './router';

